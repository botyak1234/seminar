#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <algorithm>
#include <vector>
#include <string>
#include <sstream>
#include <cstring>
#include <fstream>
#include <vector>
using namespace std;

template <typename T> T * f(T * a, int n)
{
	for (int i = 0; i < n; i++)
	{
		cin >> a[i];
	}
	return a;
}



int main()
{
	int n; cin >> n;
	int * a = new int [n];
	int *pointer = f(a, n);
	for (int i = 0; i < n; i++)
	{
		cout << pointer[i] << ' ';
	}
	delete[]a;
	delete[]pointer;
}
