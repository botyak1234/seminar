#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
#include <algorithm>
#include <vector>
#include <string>
#include <sstream>
#include <cstring>
#include <fstream>
#include <vector>
using namespace std;

int * f(int n)
{
	int * a = new int [n];
	for (int i = 0; i < n; i++)
	{
		cin >> a[i];
	}
	return a;
}



int main()
{
	int m; cin >> m;
	int* pointer = f(m);
	for (int i = 0; i < m; i++)
	{
		cout << pointer[i] << ' ';
	}
}
