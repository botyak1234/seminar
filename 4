#include <iostream>
#include <cmath>
#include <iomanip>
#include <windows.h>
using namespace std;



int main()
{
	int a, x1, x2, s ,pr, ch;
	cin >> a;
	x1 = a % 10;
	x2 = a / 10;
	s = x1 + x2;
	pr = x1 * x2;
	ch = min(s, pr);
	while (ch <= max(s, pr))
	{
		cout << ch << endl;
		ch++;
	}
}
